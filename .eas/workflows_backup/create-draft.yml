name: Create draft

on:
  push:
    branches:
      - '**'
      - '!master'

jobs:
  publish_preview_update:
    name: Publish preview update
    type: update
    environment: preview
    params:
      branch: ${{ github.ref_name || 'test' }}
  deploy_website:
    name: Deploy website
    type: deploy
